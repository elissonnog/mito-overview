# MitoOverview v0.3.0 implementation log

This log records release-hardening actions that affect the public repository. It is not a biological-results ledger; exact validation commands and outputs are captured in the versioned validation packet.

## 2026-07-20

- Confirmed branch `codex/preprint-hardening-v0.3.0` was clean at `5812799` and immutable tag `v0.2.1` remained at `2ba62b775a7204c0dc61f5408989603f536c78da`.
- Confirmed the ENA GM12878 FASTQ and full mapped BAM remained in the local validation cache after the interrupted full-depth probe; no MCW/HPC path was accessed or modified.
- Added portable public-alignment provenance and deterministic query-name subset implementation with focused known-answer and tamper-rejection tests.
- Closed reviewer-identified edge cases for absent mtDNA depth evidence, zero-support alternate alleles, auditable configured depth caps, and incomplete-reference NUMT scope assertions.
- Hardened the public validation matrix with portable cache defaults, replayable quoted commands, exact filter-profile assertions, and Python-based relative SHA-256 manifests.
- Rebuilt the GM11906 public alignment from verified FASTQs, recorded its alignment provenance, and confirmed the corrected default `m.8344A>G` result (`1027` callable depth, `740` alternate observations, `0.720545` alternate allele fraction).
- A full GM12878 BAM rebuild failed during SAMtools output merge because the Mac volume had insufficient physically writable space; the partial BAM was rejected and removed. The public long-read validation was revised to select a seeded query-name FASTQ subset directly from the complete verified run, bind that subset to the raw FASTQ by hashes, and align only the explicitly labeled reduced input. A measured 2,000-name probe averaged approximately 364 accepted observations per position but was too slow for the repeated release matrix; the locked validation subset was therefore set to 1,000 names, with an expected mean depth near 180 and no claim of analytical sensitivity.
- The first 1,000-name GM12878 probe completed corrected heteroplasmy counting with 16 descriptive candidates, 7,143,152 accepted observations, 2,047,476 accounted exclusions, and an elapsed time of 833.9 seconds. It was intentionally interrupted during the pre-existing full-span co-segregation recount and is not release evidence.
- An independent edge-case audit found that coordinate-placed records carrying the unmapped flag could be fabricated in the optimized excluded-observation provenance. The pre-count now mirrors HTSlib by skipping unmapped records, with a deterministic negative test. Co-segregation now reuses the same filter engine at exactly the selected sites rather than recounting intervening mitochondrial positions.
- A clean-commit GM12878 1,000-query-name run completed all applicable layers. It reproduced 16 descriptive candidate sites, 7,143,152 accepted allele observations, 2,047,476 accounted exclusions, 13 singleton CIGAR-deletion bins, and eight selected co-segregation sites; supplementary-alignment/SA evidence was summarized separately. Copy number and Phy-Mer were `not_applicable`; mvTool and methylation were `not_configured`; mt-only NUMT interpretation was `not_evaluable`. The run is a resource-limited workflow proof of principle, not a complete-run or analytical validation.
- Corrected deletion-screen summary accounting now counts unique query names rather than alignment records: 728 primary query names, 5 names with a large CIGAR deletion, and 542 names with a supplementary alignment or SA tag. The 13 candidate clusters remain singleton bins with maximum support fraction 0.001374.
- Completed the full public matrix at clean commit `dc09114e1a0dcec2baf83d94549dfa41f3e49c8b`: all 13 prespecified cases passed. Both normalized TSV repeat diffs and both visual-structure diffs were empty. GM11906 retained `m.8344A>G` at callable depth 1027, alternate count 740, and alternate allele fraction 0.720545. GM12878 used the provenance-bound deterministic 1,000-query-name subset and reported 16 default candidates.
- Recorded descriptive allele-filter profiles. GM11906 candidate counts were 33/33/33 under lenient/default/strict settings; accepted observations were 44,052,664/44,052,664/42,676,166. GM12878 candidate counts were 32/16/15; accepted observations were 8,278,969/7,143,152/6,046,355. These are filter-dependence descriptions, not sensitivity or specificity estimates.
- Refreshed the tracked public example summaries, representative figures, provenance manifests, and long-/short-read montages only from corrected v0.3.0 outputs. The GM12878 tracked example is explicitly labeled as a deterministic reduced workflow proof of principle.
- Reproduced the deterministic local suite after reviewer remediation (`124 passed in 18.99 s`), CLI step listing, synthetic long-read workflow, synthetic reduced short-read workflow, long-read workflow without methylation sidecars, and standalone BAM/CRAM workflows with strict dry runs.
- Hardened `REFERENCE_SCOPE=auto` so only length-consistent complete human or mouse references resolve to `whole_genome`; tiny, incomplete, and unsupported references resolve to `custom` and cannot emit categorical NUMT interpretations.
- Corrected status-only methylation provenance so extraction-created empty placeholders do not masquerade as configured source tracks. The regenerated GM12878 asset reports all four source-track presence flags as zero while preserving `status=not_configured`.
- Regenerated both tracked toy report bundles from the corrected v0.3.0 renderer and verified the zero-based mitochondrial BED, explicit NA copy-ratio semantics, mt-only NUMT suppression, canonical allele schema, and complete 14-page pattern.
- Re-ran the provenance-verified GM12878 default workflow from the cached public FASTQ subset and alignment. Filtered counting completed in 812.9 seconds with the same 16 candidates, 7,143,152 accepted observations, and 2,047,476 accounted exclusions; tracked lightweight assets were regenerated from that output.
- Added `docs/preprint_release_validation_v0.3.0.md` with methods, commands, public-input provenance, observed results, claim boundaries, pending external release gates, and an independent-auditor checklist. GitHub-hosted CI, final exact-candidate fresh-clone validation, audit ZIP, DOI, tag, and release remain pending and are not recorded as passing evidence.
