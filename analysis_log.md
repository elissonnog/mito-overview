# MitoOverview v0.3.0 implementation log

This log records release-hardening actions that affect the public repository. It is not a biological-results ledger; exact validation commands and outputs are captured in the versioned validation packet.

## 2026-07-20

- Confirmed the v0.3.0 release-candidate branch was clean at `5812799` and immutable tag `v0.2.1` remained at `2ba62b775a7204c0dc61f5408989603f536c78da`.
- Confirmed the ENA GM12878 FASTQ and full mapped BAM remained in the local validation cache after the interrupted full-depth probe; no MCW/HPC path was accessed or modified.
- Added portable public-alignment provenance and deterministic query-name subset implementation with focused known-answer and tamper-rejection tests.
- Closed reviewer-identified edge cases for absent mtDNA depth evidence, zero-support alternate alleles, auditable configured depth caps, and incomplete-reference NUMT scope assertions.
- Hardened the public validation matrix with portable cache defaults, replayable quoted commands, exact filter-profile assertions, and Python-based relative SHA-256 manifests.
- Rebuilt the GM11906 public alignment from verified FASTQs, recorded its alignment provenance, and confirmed the corrected default `m.8344A>G` result (`1027` callable depth, `740` alternate observations, `0.720545` alternate allele fraction).
- A full GM12878 BAM rebuild failed during SAMtools output merge because the Mac volume had insufficient physically writable space; the partial BAM was rejected and removed. The public long-read validation was revised to select a seeded query-name FASTQ subset directly from the complete verified run, bind that subset to the raw FASTQ by hashes, and align only the explicitly labeled reduced input. A measured 2,000-name probe averaged approximately 364 accepted observations per position but was too slow for the repeated release matrix; the locked validation subset was therefore set to 1,000 names, with an expected mean depth near 180 and no claim of analytical sensitivity.
- The first 1,000-name GM12878 probe completed corrected heteroplasmy counting with 16 descriptive candidates, 7,143,152 accepted observations, 2,047,476 accounted exclusions, and an elapsed time of 833.9 seconds. It was intentionally interrupted during the pre-existing full-span co-segregation recount and is not release evidence.
- An independent edge-case audit found that coordinate-placed records carrying the unmapped flag could be fabricated in the optimized excluded-observation provenance. The pre-count now mirrors HTSlib by skipping unmapped records, with a deterministic negative test. Co-segregation now reuses the same filter engine at exactly the selected sites rather than recounting intervening mitochondrial positions.
- A clean-commit GM12878 1,000-query-name run completed all applicable layers. It reproduced 16 descriptive candidate sites, 7,143,152 accepted allele observations, 2,047,476 accounted exclusions, 13 singleton CIGAR-deletion bins, and eight selected co-segregation sites; supplementary-alignment/SA evidence was summarized separately. Copy number and Phy-Mer were `not_applicable`; mvTool and methylation were `not_configured`; mt-only NUMT interpretation was `not_evaluable`. The run is a resource-limited workflow proof of principle, not a complete-run or analytical validation.
- Corrected deletion-screen summary accounting now counts unique query names rather than alignment records: 728 primary query names, 5 names with a large CIGAR deletion, and 542 names with a supplementary alignment or SA tag. The 13 candidate clusters remain singleton bins with maximum support fraction 0.001374.
- Completed the full public matrix at clean commit `dc09114e1a0dcec2baf83d94549dfa41f3e49c8b`: all 13 prespecified cases passed. Both normalized TSV repeat diffs and both visual-structure diffs were empty. GM11906 retained `m.8344A>G` at callable depth 1027, alternate count 740, and alternate allele fraction 0.720545. GM12878 used the provenance-bound deterministic 1,000-query-name subset and reported 16 default candidates.
- Independent adversarial review identified and remediation tests now cover four edge cases: overlap-disabled co-segregation support collapse, permissive reference-profile recognition, incomplete NUMT evidence producing a categorical label, and deletion-report wording that could conflate supplementary/SA evidence with CIGAR-bin creation. The integrated local suite passed 145 tests in 10.86 s. Exact-final-commit public reruns, CI evidence, and audit-ZIP verification remained pending; the then-planned DOI gate was subsequently removed from the GitHub-only v0.3.0 release contract.
- Recorded descriptive allele-filter profiles. GM11906 candidate counts were 33/33/33 under lenient/default/strict settings; accepted observations were 44,048,838/44,048,838/42,675,832. GM12878 candidate counts were 32/16/15; accepted observations were 8,278,969/7,143,152/6,046,355. These are filter-dependence descriptions, not sensitivity or specificity estimates.
- Refreshed the tracked public example summaries, representative figures, provenance manifests, and long-/short-read montages only from corrected v0.3.0 outputs. The GM12878 tracked example is explicitly labeled as a deterministic reduced workflow proof of principle.
- Reproduced the deterministic local suite after reviewer remediation (`124 passed in 18.99 s`), CLI step listing, synthetic long-read workflow, synthetic reduced short-read workflow, long-read workflow without methylation sidecars, and standalone BAM/CRAM workflows with strict dry runs.
- Hardened `REFERENCE_SCOPE=auto` so only length-consistent complete human or mouse references resolve to `whole_genome`; tiny, incomplete, and unsupported references resolve to `custom` and cannot emit categorical NUMT interpretations.
- Corrected status-only methylation provenance so extraction-created empty placeholders do not masquerade as configured source tracks. The regenerated GM12878 asset reports all four source-track presence flags as zero while preserving `status=not_configured`.
- Regenerated both tracked toy report bundles from the corrected v0.3.0 renderer and verified the zero-based mitochondrial BED, explicit NA copy-ratio semantics, mt-only NUMT suppression, canonical allele schema, and complete 14-page pattern.
- Re-ran the provenance-verified GM12878 default workflow from the cached public FASTQ subset and alignment. Filtered counting completed in 812.9 seconds with the same 16 candidates, 7,143,152 accepted observations, and 2,047,476 accounted exclusions; tracked lightweight assets were regenerated from that output.
- Added `docs/preprint_release_validation_v0.3.0.md` with methods, commands, public-input provenance, observed results, claim boundaries, pending external release gates, and an independent-auditor checklist. GitHub-hosted CI, final exact-candidate fresh-clone validation, audit ZIP, DOI, tag, and release remain pending and are not recorded as passing evidence.
- A second independent adversarial review found five scientific edge cases and five release-evidence gaps that remain release blockers: alignment-header scope must independently constrain whole-genome interpretation; CRAM reference identity must not depend on observed mtDNA records; exact reference profiles must reject extra contigs; co-segregation must document its shared-callable-read conditioning; mvTool must reject incomplete or non-bijective response mappings; final CI must be a push-event run on the exact candidate commit; DOI reservation needs captured record evidence; public subset/alignment provenance must enter the audit ZIP; and ZIP verification must reject normalized-path collisions.
- Confirmed the cached GM11906 alignment provenance independently matches the current BAM, index, public FASTQs, and reconstructed mitochondrial reference. The cache-local `GM11906/inputs.sha256` is stale for the BAM/index and includes an invalid self-hash, so it is not accepted as evidence; the release matrix generates a fresh manifest outside the cache.
- Removed local workspace and temporary-directory command paths from the tracked synthetic BAM headers, regenerated both BAM indexes, and added a regression test requiring path-free synthetic headers with indexed mtDNA records. No biological alignment record was changed.
- Added a deterministic release gate that scans every Git-tracked file, including binary payloads, for internal sample identifiers and local/MCW paths. The gate also checks manuscript process-wording hygiene and has text, binary, and ignored-output negative tests. Confirmed the local ignored legacy report directory has zero tracked files; it remains untouched and cannot enter package or release artifacts.
- Regenerated both synthetic report bundles after the final co-occurrence and release-hygiene changes, removed machine-specific command records from their BAM headers, verified unchanged alignment-record SHA-256 values, and passed the complete local suite (`189 passed in 14.58 s`).
- Added a guarded production Zenodo helper and versioned draft-metadata template. The helper accepts credentials only through a local environment variable, sends them only in an HTTPS bearer header, does not publish, and writes a no-clobber evidence file containing only fields required to verify the reserved DOI against the unpublished deposition.
- Passed the expanded deterministic suite after integrating the Zenodo capture-to-packet contract (`199 passed in 14.02 s`).
- Independent scientific and release-engineering reviews then identified remaining semantic and packaging issues. Remediation now marks one-site or no-valid-pair co-occurrence as `not_evaluable`, labels Figure 1C as a shared-spanning-read conditional statistic, rejects non-HTTP(S) or credential-bearing mvTool network URLs without attempting a request, and documents mitochondrial gene annotation as optional.
- Hardened the distribution and evidence chain by making the source archive self-contained for its packaged tests, bounding audit-ZIP extraction by member count, per-file size, total size, and compression ratio, preserving and cross-checking public Zenodo release metadata, pinning CI actions to exact commits, and logging exact Conda/pip inventories. No DOI was fabricated or reserved during these code-only checks.
- The integrated macOS suite passed `233` tests in `15.69 s`, and the same 233-test suite passed from the extracted source distribution; CLI listing/dry-run and all four synthetic workflows passed, including standalone BAM and CRAM. Repeated Figure 1 builds had identical SHA-256 values, the tracked toy BAM remained path-free with unchanged SAM-record SHA-256, and the removed one-site heatmap stayed absent. Exact-final-commit public reruns, push-event Linux/macOS CI, real Zenodo reservation, final audit ZIP, tag, and release remain pending external gates.
- An exact-head independent review reproduced a CRAM safety bypass when `SOURCE_ALIGN_MODE=bam` conflicted with a `.cram` input suffix. Configuration loading now rejects both BAM-to-CRAM and CRAM-to-BAM suffix conflicts before index selection, reference verification, or extraction; focused regression tests cover both directions.
- After the CRAM mode/suffix remediation, the complete deterministic suite passed `235` tests in `16.49 s`; CLI listing, generic dry-run, and release hygiene also passed.
- Re-review showed that suffix agreement alone did not reject a CRAM payload renamed with a `.bam` suffix. Alignment loading now verifies the encoded CRAM signature or decompressed BAM signature against the resolved mode before execution, with adversarial tests for renamed CRAM, renamed BAM, and a nonstandard-suffix mismatch.
- After encoded-container validation was added, the complete deterministic suite passed `238` tests in `15.82 s`; generic dry-run and release hygiene passed.
- The final malformed-container negative case increased the complete deterministic suite to `239` passing tests in `17.07 s`.

## 2026-07-21

- Verified PR 3 was clean at `782b22c45d1f6013a88c54a4f89f5b561cc835d9`, fetched public `main`, and merged `6cf41013d60199bad14ab4278c3d991898e957da` without conflict. The reconciled baseline is `6dc5f079745bf7732710a483e926ab27e6b94926`.
- Froze the post-merge `paper/` tree at `bfb5664db9c8b43ed5de33ecbddef88071fc6378`; release hardening, validation, reporting, and GitHub publication must not modify manuscript files.
- Replaced the release design with a GitHub-primary protocol. Zenodo, a DOI, bioRxiv submission, manuscript revision, Notion, and MCW/HPC deployment are outside the v0.3.0 software gate.
- Added the preregistered clean-room protocol containing locked public inputs, environment versions, scientific oracles, module/output contracts, verdict definitions, and the required release sequence.
- Integrated GitHub-only release infrastructure: exact-version environment specifications for Linux x86-64, macOS x86-64, and macOS arm64; installed-package launcher isolation; sealed-cache preparation; offline public matrix/oracle enforcement; schema 2.0 packet construction; and exact-commit GitHub Actions evidence binding.
- Verified from NCBI GEO that `SRR10804585`, `SRR10804590`, and `SRR10804657` are three C1 single-cell ATAC-seq libraries from the same GM11906 lymphoblastoid line. The public input is now explicitly documented and packeted as a three-cell, read-depth-weighted pseudo-bulk; its pooled alternate allele fraction is not interpreted as per-cell, donor-level, or calibrated sample heteroplasmy.
- Removed stale DOI/Zenodo requirements from active release-facing documentation and replaced the reproducibility ledger with the GitHub-only gate. Optional archival helper code remains outside acceptance.
- Passed the integrated deterministic suite (`208 passed`), all four synthetic smoke workflows, both synthetic example builders, wheel/sdist construction with the locally installed pinned build backend, forced fresh-wheel installation outside the checkout, the 18-step CLI registry, and strict generic dry-run from the installed wheel. The isolated PEP 517 download attempt was blocked only by the local sandbox network policy; GitHub and final clean-room isolation remain required.
- Froze commit `291df4dba4ddcb946b1ca85006e5853854394041` for three independent read-only audits covering release engineering, bioinformatics calculations, and clean-room reproducibility; the worktree and frozen `paper/` tree remained unchanged during review.
- The reviews independently reproduced two critical release-evidence defects: the public matrix selected stale columns when generating `inputs.sha256`, and the packet expected CI job identities that contradicted the pinned GitHub Actions matrix. Reviewers also found that the packet did not yet bind all seven source hashes to the frozen oracle, enforce the three-library GM11906 derivation, or prove cross-platform public-result equivalence.
- The scientific review reproduced two estimator edge cases outside the existing public oracles: supplementary-only deletion evidence could make `support_fraction_primary` exceed one, and successfully measured zero-depth nuclear windows were omitted from the mt:nuclear denominator. Remediation was assigned as a separate scientific patch with deterministic regression tests.
- Confirmed reviewers found the shared allele-observation engine, co-occurrence calculations, standalone BAM/CRAM preflight, reference-scope/BED behavior, mvTool disabled-mode behavior, GM12878 deterministic subset provenance, current tracked-tree hygiene, and frozen manuscript tree sound at the audited commit.
- Remediated all three independent-review blocker groups after `291df4d`: corrected raw-input hash columns and CI job identities; bound the packet to all seven frozen FASTQs, the sealed-cache manifest, scientific oracles, exact installed distribution, runtime versions, platform, thread count, and public cross-platform comparisons; and added packet-native release-report generation.
- Corrected two scientific edge cases with deterministic tests: supplementary-only deletion evidence can no longer make `support_fraction_primary` exceed one, and successfully measured zero-depth nuclear windows now contribute to the nuclear mean while an all-zero denominator remains `not_evaluable` rather than zero or infinity.
- Added fail-closed process-tree network isolation for the offline public matrix (`sandbox-exec` on macOS and an unshared Linux network namespace), with parent-connectivity and child-denial controls captured as packet evidence. The macOS control passed with the isolated child blocked by the operating system.
- Expanded release hygiene checks for private home paths, credential-bearing URLs, private-key headers, and plausible GitHub/AWS/assignment secrets without printing detected values. The complete tracked tree passed and the frozen `paper/` tree remained unchanged.
- Added path-free synthetic BAM generation and regression checks. Rebuilt the tracked TOY-001 bundle with identical ten alignment records and updated only the deletion-denominator and zero-depth copy-proxy report explanations. Focused bundle, packaging, scientific, and hygiene tests passed (`54 passed`).
- At commit `56c378f5cf9c6d21dae6000561efca84357fd0db`, the complete deterministic suite passed (`294 passed in 70.61 s`), all four synthetic smoke workflows and both example builders passed, installed-package CLI listing and strict dry-run passed, and the macOS network-isolation control passed. Exact-final-commit public reruns, GitHub CI, merge/tag publication, and clean-room packet/report generation remain pending release gates.

## 2026-07-21 - GitHub v0.3.0 release resumption

- Confirmed branch `codex/preprint-hardening-v0.3.0` at `9734a381e69b49db92f0a02be4d512b19f57291a` with the `paper/**` tree still frozen.
- Resumed the provisional sealed-cache public validation matrix under OS-level network isolation; GM11906 default repeat 1 completed with the prespecified `m.8344A>G` known answer and repeat 2 advanced into the lenient profile.
- Audited release-facing text for stale fixed-BAM, historical-commit, DOI, and unreleased-candidate language.
- Updated README, citation metadata, changelog, methodology, and the input/output contract to describe the seven-FASTQ per-platform reconstruction design and within-matrix report-repeatability scope. Historical and superseded records were not relabeled as final evidence.
- A read-only documentation audit identified premature final-release tense, stale historical navigation, and ambiguity between the 13-case fixed-BAM snapshot and the active 17-case seven-FASTQ gate. Release-facing files were corrected to candidate/future tense; the dated 239-test/13-case/46-file evidence was retained and explicitly labeled historical.
- The first provisional matrix PTY was severed by an application restart after two default GM11906 runs and one lenient run; its empty verdict table means it is not evidence. A fresh matrix was started under the same sealed-cache and OS-level network-isolation controls with a new work/output root.
- Added a fail-closed tracked-public-asset refresher with an explicit 56-file allowlist. It requires all 17 public-matrix cases and every scientific oracle assertion to pass, rejects symlinks, unexpected destination content, raw reads, alignments, indexes, run logs, environments, source-library metadata, and local absolute paths, and promotes staged assets atomically only after full inventory verification.
- Added deterministic tests for exact inventory, idempotence, failed or missing cases, failed oracle assertions, destination contamination, symlinks, and private-path leakage. The focused promotion, source-distribution, and release-hygiene suite passed (`39 passed in 8.28 s`), and the source-distribution contract now explicitly requires the refresher utility.
- The repository-wide hygiene gate then identified that a deliberately invalid test fixture itself contained a `/Users/...` home path after becoming tracked. The sentinel was replaced with platform-generic nonportable POSIX and Windows paths without weakening the rejection test; the focused suite passed again (`39 passed in 8.27 s`) and hygiene passed across 394 tracked files.
- A read-only release-engineering audit confirmed the local scientific/package gates but identified missing exact-PR CI provenance, explicit Ubuntu run selection, empty-cache enforcement, queried publication receipts, and an executable phased tag/release path. It also found that the ledger ordered report construction before real draft-release metadata and that `SHA256SUMS` could not hash itself.
- Corrected the release protocol, checklist, and run ledger to require the locked environment, explicit PR/push/public run identifiers, an absent raw cache, PR-head-tree-bound read-only audit records, draft-before-report ordering, inclusion of the Markdown figure directory as an archive, and a manifest that covers every other asset while its downloaded copy is checked byte-for-byte.
- Added fail-closed validation-packet binding for PR 3, the current public `main` tip, the exact Ubuntu public-validation workflow run and artifact, and the normalized macOS-versus-Ubuntu public-result comparison. The release runner now rechecks that the raw cache is an empty regular directory immediately before download preparation and repeats public-main verification in the fresh clone.
- Expanded adversarial tests to reject a different PR number, a stale public-main candidate, incomplete or mismatched public-workflow evidence, an expired or wrong artifact, unequal normalized cross-platform results, and resealed packet tampering. The focused runner/packet suite passed `119` tests.
- Added a three-phase GitHub release publisher (`create-draft`, `upload-verify`, and `publish`) that verifies exact remote `main`, the annotated tag object and peeled target, canonical asset hashes after authenticated redownload, and the queried published-release state. It does not claim an attestation that was not performed.
- Re-ran the complete deterministic suite after integrating these evidence-chain changes; all `384` tests passed in `103.11` seconds. The frozen `paper/**` tree remained `bfb5664db9c8b43ed5de33ecbddef88071fc6378`.
- Completed the provisional seven-FASTQ public matrix under macOS process-tree network isolation. All `17/17` required cases and `256/256` frozen oracle assertions passed; GM11906 and GM12878 normalized repeat diffs and visual-structure diffs were empty, and all six filter-profile counts matched the preregistered oracle.
- Refreshed the exact 56-file tracked public-example inventory only after those gates passed. Visual review found clipping in the earlier long-read montage; the montage composer was corrected to use bounded caption bands, trimmed report-native panels, and protected margins without modifying any scientific source plot or manuscript file.
- Two subsequent read-only audits found finish-line defects rather than scientific-oracle failures: acceptance rows were generated before Ubuntu evidence existed; cross-platform rows were not bound to downloaded files; audit comments lacked authenticated poster and execution-instance identity; remote `main` was not rechecked at packet finalization; the source distribution omitted GitHub workflow contracts; and the publisher could not safely execute the approved tag-first draft/report/release sequence.
- Remediated the packet path by verifying the downloaded Ubuntu `SHA256SUMS`, exact artifact inventory, Linux/run/commit identity, every normalized scientific file hash, and every visual-inventory structure in both the builder and fresh-extraction verifier. Added unique role-audit execution IDs with authenticated owner-posted comment metadata, moved acceptance evaluation after Ubuntu evidence, and rechecked public `main` immediately before packet construction and receipt generation. The expanded packet suite passed `92` tests and the runner suite passed `34` tests.
- Reworked the publisher to require a pre-existing annotated tag, enumerate authenticated drafts, resume partial uploads and post-publish verification, preserve exact tag-object identity, enforce the canonical asset inventory, emit report-compatible path-free receipts, and record the irreversible transition before fallible post-publish checks. Following current GitHub documentation, repository immutable releases are enabled through the documented `PUT` endpoint when the initial `GET` reports disabled, and the final release must report `immutable=true`. The focused publisher suite passed `23` tests without a live GitHub mutation.
- Added both GitHub Actions workflow contracts to the source distribution; the isolated sdist inventory and focused packaging test passed. Final repository-wide, extracted-sdist, CI, clean-room, tag, release, packet, and report gates remain pending until these changes are committed and independently re-audited.
- An initiated three-role audit of commit `910db532b1653e539dd636b5c4ae5246fc54131a` stopped release progression. The completed bioinformatics and release-engineering roles reported four concrete blockers; the reproducibility role correctly paused when it detected the remediation tree changing. The blockers were: empty deletion runs emitted a headerless read-flags TSV; exact deletion breakpoint/bin coordinates lacked a known-answer regression; packet sanitization could mutate copied Ubuntu evidence after its embedded `SHA256SUMS` was verified; and report/release publication did not enforce fully verified metadata plus the documented post-tag public-clone check.
- Remediation preserves the four-column deletion read-flags schema for zero rows and protects exact one-based inclusive deletion coordinates and 10-bp bin outputs. The public GitHub workflow now sanitizes derived text evidence before hashing/upload, while packet construction treats the downloaded Ubuntu artifact as immutable and re-verifies its original manifest after packet path normalization.
- Added `run_fresh_public_tag_validation_v0.3.0.sh` and a hash-manifested receipt contract. The runner requires a public HTTPS clone of the annotated tag, exact peeled `FINAL_SHA`, pinned environment, wheel/sdist build, installed-package CLI outside the checkout, complete source-distribution tests, four synthetic smoke modes, both example builders, and a clean detached worktree. The GitHub publisher fails before mutation if this evidence is absent, non-PASS, path-leaking, or hash-inconsistent.
- Hardened the report builder so unverified, transition-only, tag-inconsistent, immutable-state-inconsistent, incomplete post-publication, or divergent asset-verification receipts cannot yield a passing release report. Focused remediation suites passed: packet/scientific/publisher integration `163 passed`, complete pre-publication-control tree `402 passed`, report receipt checks `19 passed`, publisher receipt checks `24 passed`, and combined runner/publisher/report/packaging checks `54 passed`. The final remediation tree then passed the complete `409`-test suite in `119.30` seconds, all four synthetic smoke workflows, both example builders, and exact 14-page HTML inventory checks. All three fresh role audits remain required because the candidate tree changed.
- The first post-remediation audit SHA was withdrawn before push when a local end-to-end wrapper check showed that the new public-tag runner incorrectly expected `report/index.html`; example bundles intentionally expose 14 numbered pages. The runner now requires exactly 14 HTML files plus nonempty first and final numbered pages for both modes, and its static contract test explicitly rejects the nonexistent index assumption. No scientific output or manuscript file changed.
- The same withdrawn audit found that fresh-tag evidence and the live remote tag could identify different annotated tag objects while peeling to the same commit. Every publication phase now cross-binds the evidence `tag_object_sha` to the current remote annotated-tag object before any hosting-state, upload, or release mutation; repeated pre/post-publication tag queries enforce the same binding.
- Scientific contract review normalized state-dependent TSV schemas: copy-number windows now always include `valid_for_denominator`, co-segregation pairwise columns retain one canonical order, and populated identity fingerprints are restricted to the same six declared fields used by empty/status outputs. CIGAR-deletion breakpoint clusters now define zero-based 10-bp anchors with explicit exact-boundary tests; one-based inclusive event coordinates remain unchanged.
- The public-tag runner now has a network-free executable control-flow test: an annotated local fixture exercises all 12 PASS cases, receipt generation, tag-object binding, path sanitization, and independent evidence-hash checks, while a 13-page fixture must fail at `example_builders` without a PASS receipt. Fresh-tag environment evidence also records the observed operating system and architecture.
- The corrected tree passed the complete `414`-test suite in `125.25` seconds and all four checkout-backed smoke workflows (long-read, reduced short-read, long-read without methylation, and standalone BAM/CRAM). Both public example builders were regenerated from checkout code; the only tracked artifact changes were the three normalized TSV headers and the deletion-method report text, while both bundles retained 44 summary TSVs and 14 HTML reports with no local or HPC paths.
- Independent review remediation added explicit missing-evidence denominators and module states for exploratory methylation and circularity metrics, preserved GTF CDS phase, rejected split or non-phase-zero CDS consequence inference, hardened sealed-cache and network-isolation controls, and cryptographically bound release assets to fresh-tag evidence. The GitHub publication sequence was also made non-self-referential: a read-only prepublication identity receipt precedes report construction, while the final publication receipt remains a separate post-upload artifact.
- Regenerated the corrected synthetic bundles from checkout code in new temporary workspaces. The long-read bundle changed exactly seven report/summary files and the reduced short-read bundle exactly two feature-annotation files; candidate tables, BAMs, PNGs, and all other outputs remained byte-identical. Both bundles retain 44 summary TSVs and 14 HTML reports; the long-read and short-read bundles contain 20 and 9 PNGs, respectively. Added a TSV-specific Git whitespace attribute so an empty final tab-delimited field is not misreported as formatting damage.
- The first broad local test invocation intentionally failed closed with `2 failed, 476 passed` because a checkout-only Python shim had been exported into two network-isolation tests and refuses isolated `-I` execution. The two affected tests passed under the pinned interpreter (`2 passed`), and the corrected full invocation with the shim scoped only to workflow smokes passed `478` tests in `136.00 s`.
- All four checkout-backed workflow gates passed after the corrected unit-test run: synthetic long-read, reduced short-read, long-read without methylation, and standalone BAM plus CRAM. Both synthetic example builders also completed successfully before tracked-output promotion.
- Built `mito_overview-0.3.0-py3-none-any.whl` and `mito_overview-0.3.0.tar.gz` with the pinned build environment. A forced fresh-wheel installation imported from the temporary virtual environment rather than the checkout or base environment, exposed all 18 CLI steps, and completed the generic dry-run. The extracted source distribution independently passed all `478` tests in `135.19 s`.
- Final local hygiene passed for 404 tracked files, `git diff --check` passed under the tab-delimited-file rule, and the frozen `paper/**` tree remained `bfb5664db9c8b43ed5de33ecbddef88071fc6378`. GitHub PR/merge CI, exact-final-commit public reproduction, cross-platform comparison, audit-packet/report construction, annotated tagging, and release publication remain later gates rather than inferred successes.
- Three role-separated read-only audits of `232b3af8e18d2a714dda49280d6b6ee1d3351bc0` identified finish-line scientific and evidence blockers. The scientific audit found that zero-callable-depth sites were serialized as an observed zero fraction, duplicate bedMethyl coordinates could inflate position denominators, incomplete depth profiles could yield an `ok` circularity result, and deletion-bin anchors could be reused as biological feature-overlap intervals. The reproducibility and release audits independently found unsafe packet symlink traversal and a missing semantic link between prebuilt report assets and `FINAL_SHA`.
- Scientific remediation now writes undefined zero-depth allele fractions as `NA`, records callable and uncallable position counts, pools duplicate bedMethyl rows by modified/canonical calls, requires a complete unique in-range depth profile for circularity metrics, and calculates deletion-cluster feature overlap only from exact one-based member-event intervals. Deterministic negative tests cover each correction. In the toy methylation fixture, the internally inconsistent supplied `75%` value with counts `7/3` is now recomputed as `70%`; this is a count-derived correction rather than a renderer change.
- Validation-packet construction and its generated verifier now reject file, directory, broken, and external symlinks; nonregular filesystem entries; and paths resolving outside their declared source roots. Packet copying uses no-follow file descriptors on the supported Linux/macOS release platforms.
- Fresh-tag validation now safely extracts and executes the packet verifier, cross-checks packet and adjacent receipt identities against the repository and exact commit, requires a complete report-asset size/SHA-256 manifest, rejects stale-commit bundles or post-manifest substitutions, and machine-enforces the frozen `paper/` tree.
- Added `scripts/assemble_release_assets_v0.3.0.py` to remove the remaining manual release handoff. It atomically creates the exact nine-file prebuilt asset source, builds deterministic report-figure and three-platform environment-lock archives, validates commit identity in Markdown, DOCX, release notes, environment records, and platform lock metadata, executes the packet verifier, embeds the report-asset manifest, and rechecks the finished bundle before publication. Eight deterministic happy-path, stale-identity, incomplete-lock, symlink, and reproducibility tests passed.
- Focused post-remediation suites passed: scientific plus packet safety `131 passed in 84.49 s`, and the connected assembler/fresh-tag/release-runner/publisher/report/packet slice `228 passed in 123.37 s`.
- Regenerated the tracked long-read and reduced short-read toy bundles twice in independent temporary workspaces. All 88 long-read files and all 74 short-read files were byte-identical between rebuilds. Exactly 22 tracked artifacts changed, confined to corrected heteroplasmy, methylation, circularity, and deletion-member-overlap summaries, HTML pages, and their dependent PNGs.
- The corrected working tree passed the complete `512`-test suite in `166.86 s`. Synthetic long-read, reduced short-read, long-read without methylation, and standalone BAM/CRAM smoke workflows all passed. Wheel and sdist construction passed; the fresh wheel imported from temporary `site-packages`, exposed 18 CLI steps, and completed the generic dry-run outside the checkout. The extracted sdist independently passed all `512` tests in `170.91 s` and contained the release assembler plus its tests.
- These are local candidate gates, not final release evidence. New exact-SHA read-only audits, PR CI, merge/push CI, empty-cache macOS and Ubuntu public reproduction, cross-platform comparison, packet/report generation and visual QA, annotated tag validation, and GitHub release publication remain required. No MCW/HPC, Notion, manuscript, Zenodo, DOI, or bioRxiv action was performed.
- Three fresh role audits of `2c40cae3927f999d82f7b8b4c3a7fe882261764f` stopped release progression. They found five finish-line blockers: the publisher omitted the fresh-tag runner's `release_asset_semantic_identity` case; missing feature annotation could become a biological `intergenic_variant`; bedMethyl pooling could mix modification codes or strands; report figures/PDFs lacked a machine-verifiable packet-to-render provenance chain; and environment-lock assembly accepted a four-file subset plus unrecognized extras although CI emits five files per platform.
- Scientific remediation propagates upstream `not_configured`, `not_applicable`, and `not_evaluable` feature states and assigns `intergenic_variant` only after successful annotation with genuine no-overlap evidence. Methylation rows now preserve `modification_code` and `strand`; pooling and NP/proxy comparisons require identical modification identity. The publisher requires the same independently enumerated 14 cases as the fresh-tag runner.
- Added a three-stage report evidence chain. The report builder emits `report_build_provenance.json` with packet, Markdown, DOCX, manifest, and packet-native figure hashes. The finalizer re-extracts and verifies the validation ZIP, compares every figure directly with its packet source, binds the rendered PDF and contiguous page PNG inventory to the source DOCX, and requires an explicit all-pages visual-review PASS. Assembly and fresh-tag verification reopen the report-assets archive and recompute these identities; stale PDFs, altered figures/pages, unsafe paths, false visual reviews, and inventory drift fail closed.
- Tightened resolved-environment evidence to an exact 15-file contract: `conda`, `pip`, environment YAML, platform JSON, and Python-version text for each of `linux-64`, `osx-64`, and `osx-arm64`. Unexpected files, unresolved platform records, or any Python version other than `3.12.13` are rejected.
- An initial synthetic rebuild accidentally selected the older installed package because the raw pinned interpreter passed the launcher's isolated import check. Those outputs reverted corrected NA/status schemas, were recognized by comparison, and were discarded without promotion. Rebuilds were repeated through the checkout-only Python shim. The two independent long-read builds were byte-identical across all 88 files, and the two independent short-read builds were byte-identical across all 74 files. Exactly six long-read methylation assets changed to add modification-code/strand identity; numerical methylation results were unchanged, and the short-read bundle was unchanged.
- The integrated checkout passed `536` tests in `170.66 s`, all four synthetic workflows, both deterministic example rebuild comparisons, the 18-step CLI registry, strict generic dry-run, shell/Python syntax checks, release hygiene for the then-tracked tree, and the frozen `paper/**` tree check. Exact-candidate independent re-audits, distributions, PR/push CI, public clean-room reproduction, packet/report rendering, tag, and GitHub release remain pending and are not inferred from these local results.
- Fresh exact-tree audits rejected candidate `6d665660cf23e375481820b1f0117373d3290c9b` despite its passing suite. Reproducibility review found that the sdist was extracted/tested but not separately installed, rendered PNG pages were not cross-checked against the PDF page tree, and resource evidence omitted declared input/output byte volumes. Scientific review found a modkit denominator error when `N_other_mod` was nonzero, collapsed overlapping mitochondrial CDS annotations, incorrect ordinary-codon notation at alternative mitochondrial starts, and several missing-evidence paths represented as successful zeros. A smaller cache-finalization interruption window was also accepted for remediation. This withdrawn SHA was not pushed.
- Scientific remediation now computes modkit percentages from `N_mod + N_other_mod + N_canonical`, preserves modification identity, emits every overlapping mitochondrial feature in stable order, treats accepted vertebrate mitochondrial initiators as methionine at amino-acid position 1, reports disrupted starts conservatively as `start_lost`/`p.M1?`, and distinguishes missing upstream evidence from observed zero across feature annotation, identity QC, gene summary, mito QC, and deletion support. Targeted scientific regression tests passed `74/74`.
- Release remediation now installs wheel and sdist in separate environments outside the checkout, requires both installation receipts in the packet and fresh-tag contract, compares contiguous rendered PNGs with the PDF page-tree count, records declared-input inventory bytes and changed/new validation-output bytes with an explicit method identifier, and resumes the manifest-written/seal-missing cache state without network access. The connected release, packet, report, publisher, and cache suite passed `274/274`.
- Candidate `b202354b274c3718cab9261d186551fd49794f51` regenerated the corrected synthetic long-read and reduced short-read bundles twice from checkout code. The independent long-read rebuilds were byte-identical across all 88 files, and the independent short-read rebuilds were byte-identical across all 74 files. The promoted differences are limited to audited status/NA propagation, deletion summary state, the corrected methylation denominator schema, and their report-native derivatives.
- The complete checkout suite passed `562` tests in `173.19 s` after the corrected bundle promotion. All four workflow gates passed with explicit exit-code-zero evidence: full synthetic long-read, reduced short-read, long-read without methylation sidecars, and standalone minimal BAM plus CRAM.
- Isolated PEP 517 construction produced `mito_overview-0.3.0-py3-none-any.whl` (SHA-256 `d9374aa757c812eab6aa3b4820739c9b4e658887dcc667a0c336910ee9c175a2`) and `mito_overview-0.3.0.tar.gz` (SHA-256 `ee2fa00541f8a2dad7921d66ad2110577bab9a5899774a50b21133eaee929d0e`). The first sandboxed build attempt failed only because isolated build requirements could not resolve through restricted DNS; the repeated isolated build with network permission completed without disabling PEP 517 isolation.
- Wheel and source distribution were installed independently into separate Python 3.12 environments outside the checkout. Each imported version `0.3.0` from its own `site-packages`, exposed all 18 CLI steps, and completed the generic dry-run with empty `PYTHONPATH`. A fresh extraction of the built source distribution independently passed all `562` tests in `178.57 s`.
- The frozen `paper/**` tree remained `bfb5664db9c8b43ed5de33ecbddef88071fc6378`. Fresh exact-candidate role audits, PR/push CI, empty-cache public reproduction, packet/report generation, tag validation, and GitHub release publication remain pending and are not inferred from these local gates.
- Three independent read-only audits of candidate `3e9304dee962a08cd308d496a1434495589d3746` identified four additional release blockers. Fresh-tag validation compared only selected synthetic files rather than the complete 88-file long-read and 74-file short-read inventories; the wheel omitted packaged annotation resources; resource-byte labels overstated their measurement scope; and the GM11906 source identity was supported only by project-authored metadata. The scientific audit also found that missing identity/gene evidence could be serialized as observed zero, the public examples retained pre-remediation schemas, and the NUMT/full-length metric mixed primary and all-alignment denominators.
- Remediation made fresh-tag validation compare exact complete synthetic inventories, packaged the mitochondrial GTF and reference FASTA in the wheel, and added installed-resource lookup tests. Resource metrics now explicitly report broad declared-input bytes and changed/new output bytes rather than claiming operating-system I/O accounting. A tracked NCBI metadata snapshot binds the three GM11906 SRR accessions to their GEO samples and cell-line identity through official GEO SOFT and SRA XML records, with source URLs, retrieval timestamp, and SHA-256 values propagated into cache, matrix, packet, report, and verifier contracts.
- Scientific remediation now distinguishes unavailable identity and gene-level quantitative evidence from observed zero, derives the long-read full-length fraction only from primary alignment records, cross-checks the same denominator in alignment-ambiguity QC, and marks that metric `not_applicable` for short-read mode. The corrected synthetic bundles were regenerated twice; all 88 long-read files and all 74 short-read files matched byte for byte between independent builds and were promoted only after focused package/release tests passed.
- Re-ran the corrected GM11906 and GM12878 default public workflows from provenance-verified local alignments without modifying the sealed raw cache. GM11906 retained the preregistered `m.8344A>G` result: callable depth `1027`, alternate count `740`, forward/reverse counts `305/435`, alternate allele fraction `0.720545`, `MT-TK`, and `tRNA_variant`. GM12878 retained `16` candidates, `7,143,152` accepted and `2,047,476` excluded observations, `728` primary and `543` supplementary records, mean/median depth `545.484/544`, eight selected co-occurrence sites, 13 CIGAR-deletion bins, five deletion-supporting query names, and 542 supplementary/SA query names. Both default bundles retained 44 summary TSVs and 14 HTML pages; GM11906 and GM12878 retained seven and 15 PNGs, respectively. A combined matrix containing the corrected defaults passed all `256/256` frozen scientific assertions before the fail-closed 56-file tracked public-example refresh.
- Visual inspection found that co-segregation heatmap labels used white text on bright viridis cells because the annotation-contrast condition was reversed. The renderer now uses black text for bright cells and white text for dark cells, with a focused threshold test. The corrected GM12878 report-native heatmap and montage were visually inspected, and two long-read toy rebuilds remained byte-identical across all 88 files because the one-site toy correctly has no evaluable heatmap.
- A fresh role-separated audit of candidate `9c7a5ceea3d04ebdb160dbca1b4c584e160578f3` stopped the PR push because missing heteroplasmy/feature evidence could still become successful identity or gene-level zeros, a missing feature-overlap row could become `intergenic`, raw reference span could count large CIGAR deletions as full-length evidence, and an empty deletion-cluster maximum was serialized as zero. The reproducibility audit also found that packet/verifier state vocabularies omitted the runtime `failed` state.
- Remediation now requires explicit upstream heteroplasmy and feature-summary states before zero-site interpretation, enforces exact candidate-to-feature key coverage before consequence annotation, represents undefined deletion-cluster maxima as NA with status/reason fields, and synchronizes all six module states across runtime and packet verification. Long-read near-complete alignment evidence now uses primary records and sums only CIGAR `M`, `=`, and `X` bases; `D/N` are excluded. The historical `primary_full_length_fraction` field remains a compatibility name and is explicitly documented as an alignment-completeness descriptor rather than intact molecule length.
- Provisional provenance-checked reruns from local cached public inputs preserved the scientific oracles after remediation. GM11906 retained 33 candidates, 44,048,838 accepted and 7,296,932 excluded observations, with `m.8344A>G` at `740/1027 = 0.720545`. GM12878 retained 16 candidates, 7,143,152 accepted and 2,047,476 excluded observations, eight selected co-occurrence sites, 13 CIGAR-deletion bins, and mt-only NUMT interpretation `not_evaluable`. The corrected near-complete aligned-reference result was `25/728 = 0.0343`, replacing the misleading reference-span value `473/728 = 0.6497`. These cached reruns support asset regeneration only; exact-final-commit empty-cache macOS and Ubuntu reproduction remains mandatory release evidence.
- These refreshed examples and local checks are candidate evidence only. Exact-candidate full tests, workflow gates, package installations, independent re-audits, PR and push CI, empty-cache macOS and Ubuntu reproduction, packet/report construction, annotated tag validation, and GitHub release publication remain pending and must not be inferred from this entry.
- The post-audit remediation suite passed `593` tests in `190.90 s`. Python compilation, shell syntax, release-diff hygiene, and the frozen `paper/**` tree check passed; all four end-to-end workflows also passed: full synthetic long-read, reduced short-read, long-read without methylation sidecars, and standalone minimal BAM plus CRAM.
- The corrected long- and short-read synthetic builders reproduced the exact 88-file and 74-file tracked inventories. Focused scientific tests passed `91/91`, and the tracked-public-asset refresher tests passed `9/9`.
- A new provisional public matrix replaced both default repetitions with the corrected GM11906 and GM12878 outputs. All `256/256` frozen scientific assertions passed before the fail-closed 56-file asset refresh. Visual inspection confirmed readable alignment-ambiguity axes, heatmap annotations, and montage layout. This remains local cached-input evidence only; empty-cache exact-final-commit macOS and Ubuntu reproduction is still mandatory.
- Independent review of candidate `44109471880af49972704855ff5974ea722e0537` identified BAM-record-order dependence when overlapping mates tied on BaseQ and MAPQ. The shared observation engine now excludes discordant top-quality ties as ambiguous and uses read 1, then read 2, then a stable alignment key for concordant representative-fragment ties. Exact tests cover both physical BAM orders and both read-1 orientations; strand counts are documented as representative-fragment support rather than independent mate evidence. The complete suite passed `767` tests before public-matrix execution.
- The first matrix against the stale oracle failed closed on exactly `22/366` assertions, all confined to predeclared GM11906 accepted/excluded totals and dependent fingerprints. Total observation accounting was conserved: default/lenient moved 3,826 selected observations to exclusion (`7,652` ambiguous records from `3,543` query names), and strict moved 334 (`668` records from `331` names). Candidate counts and the `m.8344A>G` result remained unchanged; GM12878 had zero overlap ambiguities and no scientific-oracle drift.
- Two independent reviewers derived the corrected oracles from normalized outputs and found no unexplained delta. Oracle commit `0bd64d2eed7400cd8772e77504b8ceab1f668cd8` then completed all 17 matrix cases and all `366/366` assertions under macOS process-tree network denial. Same-platform normalized TSVs, decoded pixels, and HTML structure matched across default repeats; cache pre/postflight passed. These remain pre-push candidate results until repeated from the final public commit and empty cache on macOS and Ubuntu.
- Three exact-tree audits of candidate `4343909035b76be61b8a23611a836c40dbfdcd8e` confirmed the five scientific corrections and evidence invariants but stopped publication for two correctable release-contract issues. Repository-only Zenodo helper tests and files still participated in the mandatory pytest/sdist gate despite archival tooling being outside scope, and the focused GM11906 mpileup used default BAQ/counting behavior that yielded depth `830` rather than the workflow's callable depth `1027`.
- Release acceptance now targets only `tests/`; 14 mocked archive-helper tests remain explicitly runnable under `optional_checks/` but are excluded from CI acceptance and source distributions. The mandatory collection contains 757 tests. README and CHANGELOG versions are now included in the packet's metadata-agreement and extracted-verifier contracts.
- The focused GM11906 mpileup now uses `-A -B -d 0 -Q 13 -q 20 --ff 3844` with overlap removal enabled. On the provenance-verified public alignment it reproduces `A/C/G/T = 285/0/740/2`, depth `1027`, and alternate count `740`. Documentation labels this an inspection artifact because mpileup does not implement the complete mean-read-quality and deterministic tie-resolution contract and is not independent caller validation.
- A first live macOS rerun exposed a Bash 3.2 `set -u` portability error when the default overlap mode expanded an empty optional-argument array. The run stopped after creating a provenance-bound alignment and before allele analysis, so it was not counted as a pass. The argument list was changed to a nonempty base array with conditional `-x`; a second offline run from the sealed public FASTQs then completed all modules. It reproduced 33 candidates, 44,048,838 accepted and 7,296,932 excluded observations, and `m.8344A>G` at depth/alternate count `1027/740`, forward/reverse `305/435`, and fraction `0.720545`. All 45 normalized files matched the reviewed default-run evidence, with 44 summary TSVs, 14 HTML reports, and seven PNG figures.
- Three role-separated read-only audits of candidate `a3ee1b342ce27468fad4f63533ae50d5c94d6a79` found evidence-integrity blockers rather than scientific-oracle drift. The packet/report path contract disagreed on the cross-platform table location; resource measurements were not bound to exact commands, logs, case inventory, or commit; decoded-pixel rows were not recomputed from packaged run-1 and run-2 PNGs; nested files named `artifacts.sha256` escaped the root manifest; resolved CI environments were not semantically packet-bound; and structured audit comments lacked pre-merge timestamp enforcement. A low-severity identity-QC presentation path also described unreadable VCF evidence as a successful zero or as merely unconfigured.
- Remediation now copies the cross-platform table to the packet root, binds all 11 resource cases to exact candidate/command/log hashes and four threads, decodes both repeat PNG sets to canonical RGBA, manifests every nested regular file, validates all three exact CI environment/lock records, and requires owner-authenticated audit `created_at`/`updated_at` values no later than merge. Case-insensitive UUID reuse, self-consistent lock substitution, command relabeling, fake pixel hashes, run-2 image drift, post-merge audit edits, and nested-manifest tampering all have fail-closed tests. Identity-QC logs and figures now preserve `not_evaluable` versus `not_configured` reasons.
- The connected reviewer-targeted suite passed `202` tests, followed by the complete checkout suite at `619 passed in 230.07 s`. The suite regenerated and byte-compared all 88 long-read and 74 reduced-short-read tracked synthetic artifacts. CLI listing exposed 18 steps, the generic dry run passed, and all four checkout-backed workflow gates passed: full synthetic long read, reduced short read, long read without methylation, and standalone BAM plus CRAM. These remain local pre-audit gates; an exact clean commit, fresh read-only audits, PR/push CI, empty-cache macOS and Ubuntu reproduction, packet/report visual QA, tag validation, and GitHub publication remain mandatory.

## 2026-07-21 - Public-derivation provenance audit remediation

- Froze candidate `bb2c63228baea418526e14e3eaa550d704b4435e` after the archive-gate and focused-mpileup remediation. Release-engineering and scientific audits found no code blocker, but the independent reproducibility audit correctly stopped release progression on two evidence-integrity defects.
- Reproduced the first defect: live subset verification checked hashes, counts, and declared seed but did not recompute whether the ledger contained the actual 1,000 minimum SHA-256-scored query names. A different self-consistent ledger and subset could therefore pass metadata-only packet verification.
- Reproduced the second defect: alignment provenance stored command templates, parameters, and tool versions, but cached-alignment reuse and packet extraction enforced only the derivation identifier. This allowed a self-consistent record to omit or drift the actual derivation details.
- The FASTQ subset verifier now checks the source digest first, rescans all source records, recomputes the seeded minimum-name set, requires the exact canonical ledger, verifies exact subset FASTQ membership without duplicate names, and enforces the complete selection-field inventory.
- Public alignment verification now requires exact command templates, sorted string parameters, unique tool identities, and observed locked tool versions for both record and reuse operations. The GM11906 runner binds BWA/samtools with four threads; the GM12878 runner binds minimap2/samtools, four threads, unmapped flag 4, selected-name count 1,000, and the frozen seed.
- Packet construction and `verify_bundle.sh` now pin the reviewed GM12878 subset FASTQ (`40e203...df33`), selected-name ledger (`3444cc...a19`), complete selection metadata, and exact short- and long-read derivation dictionaries. The long alignment must link the source FASTQ, subset FASTQ, subset manifest, and selected-name ledger as four distinct inputs.
- Added negative tests for nonminimum ledgers, wrong subset membership, changed command templates, parameters, and tool versions, and self-consistent packet resealing. Focused provenance tests passed `10/10`; the public protocol passed `78/78`; ten positive/adversarial packet cases passed; and the complete mandatory suite passed `771/771` in 380.57 seconds. Optional archive-helper checks remained separate and passed `14/14`.
- A diagnostic GM12878 run correctly passed the new provenance checks but exposed that the provisional Python still contained an older installed `0.3.0` wheel. Its legacy-schema outputs were retained only as diagnostic evidence and excluded from release evidence. A new wheel was built from the corrected working tree and installed in an isolated environment with `MITO_OVERVIEW_REQUIRE_INSTALLED=1`.
- The current-wheel GM12878 default rerun rescanned all 193,043 source records, reproduced the frozen ledger hash, completed all applicable modules, and matched the reviewed normalized scientific tables and visual inventory exactly. It produced 44 summary TSVs, 14 HTML reports, and 15 PNGs; 16 candidate sites; 7,143,152 accepted and 2,047,476 excluded observations; 13 deletion clusters; eight co-occurrence sites; and mt-only NUMT interpretation `not_evaluable` with `reference_scope_mt_only`.
- The current-wheel GM11906 default rerun verified the exact BWA derivation and matched the reviewed normalized tables and visual inventory exactly. It produced 44 summary TSVs, 14 HTML reports, and seven PNGs; 33 candidates; 44,048,838 accepted and 7,296,932 excluded observations; and `m.8344A>G` at callable depth 1,027, alternate count 740, forward/reverse 305/435, and alternate allele fraction 0.720545.
- All four installed-wheel synthetic workflows passed. Both example bundles were built twice and matched each other and the tracked 88-file long-read and 74-file short-read bundles byte-for-byte. Wheel/sdist installation, 18-step CLI listing, strict dry-run, 43 focused packaging/hygiene tests, and the 433-file release-hygiene scan passed. The frozen `paper/**` tree remained `bfb5664db9c8b43ed5de33ecbddef88071fc6378`.
- This remediation remains a local pre-commit checkpoint. A new exact commit, three fresh read-only audits, PR-head CI, merge/push CI, empty-cache macOS and Ubuntu reproduction, packet/report verification, annotated tag, and GitHub release remain mandatory and must not be inferred from these local passes.

## 2026-07-22 - Exact-audit provenance and narrative-evidence remediation

- Froze and reviewed candidate `95801108c9b26178c28bd7a6cd59d9c67121c4a3` (tree `b4fcd53e7f071c4a995bcdaf6378be7ae2839ce7`) with separate release-engineering and reproducibility agents. The candidate was held and not pushed after four independently reproducible release-evidence defects were found.
- Reproduced a real selected-ledger linkage mismatch: deterministic subset provenance omitted MD5 while alignment provenance included it, so a genuine GM12878 packet could not satisfy exact record linkage. Selected-name records now include MD5 consistently; the frozen ledger identity is 18,422 bytes, MD5 `64d606e56bf8dd58ad68baad28898e18`, and SHA-256 `3444cc7db3dcf78bea807d8bcc6686883a7759d128288c1d26aeae077a771a19`.
- Reproduced checkout import leakage in the public alignment and deterministic-subset helper scripts. In `MITO_OVERVIEW_REQUIRE_INSTALLED=1` mode, all three provenance/subset helpers now avoid inserting the checkout and fail if their validation module resolves from it. Dynamic isolated-interpreter tests prove an external package wins over checkout code.
- Reproduced duplicate-label collapse in live alignment verification, packet construction, and the extracted packet verifier. All three layers now require nonempty, unique labels and reject duplicate, malformed, missing, or unexpected input records before set comparison.
- Reproduced that `claim_evidence_matrix.tsv` and `manuscript_handoff.tsv` were structurally hashed but not semantically bound. Packet construction and fresh extraction now require the exact six bounded claims and limitations, resolve each evidence token to a PASS case or required file, and rederive every handoff metric/value/unit/source/boundary row from validated `filter_profile_results.tsv`.
- Added adversarial tests for duplicate labels, selected-ledger MD5 linkage, checkout import leakage, unsupported claim substitution, forged handoff values, and rehashed packet resealing. The directly affected suite passed `226/226`; the complete repository suite passed `783/783` in 396.52 seconds.
- Built wheel and sdist from the remediated tree, force-installed the wheel in an external venv, and verified both package and provenance module paths resolved under that venv. All three helper import probes and all four installed-wheel workflow modes passed.
- Rebuilt temporary GM11906 and GM12878 derivatives from the existing verified raw FASTQs without modifying the cache. The regenerated GM12878 subset and alignment manifests carry the same selected-ledger MD5/SHA-256, and both public normalized TSV sets and visual structures match the prior reviewed outputs exactly.
- GM11906 retained 44 summary TSVs, 14 HTML reports, seven PNGs, 33 candidates, 44,048,838 accepted observations, 7,296,932 excluded observations, and `m.8344A>G` at depth/count/forward/reverse/fraction `1027/740/305/435/0.720545`. GM12878 retained 44 summary TSVs, 14 HTML reports, 15 PNGs, 16 candidates, 7,143,152 accepted observations, 2,047,476 excluded observations, 13 deletion clusters, eight co-occurrence sites, and mt-only NUMT interpretation `not_evaluable`.
- This remains a working-tree remediation checkpoint. It requires a new exact commit, complete exact-tree rerun, three fresh PASS audits, PR-head CI, and all subsequent clean-room/release gates. Candidate `95801108...` remains rejected evidence and will not be represented as release-ready.

## 2026-07-22 - Nested provenance identity remediation

- Candidate `b639eec19f1e30add780eb4a74c48ab8aeebee8e` (tree `c64de25bcdaa8e4e41b1ebbd23e7b7d4d186a491`) was held and not pushed after fresh release-engineering and reproducibility audits found three evidence-contract gaps. The tracked GM12878 alignment manifest still recorded the pre-update subset-manifest size and digests; live FASTQ-subset verification accepted incomplete digest-field inventories; and the extracted packet verifier did not independently anchor nested provenance schema, record type, and dataset identity.
- The tracked alignment record now binds the current subset manifest as 1,180 bytes, MD5 `6f513c649b42ff758e04410b60c988f5`, and SHA-256 `82b675b95b5a0123ed0b028168acae0b63c6d6cbadc46e13391adcb5fba675c5`. A repository regression test recomputes those values from the tracked file and requires exactly one matching labeled input.
- Deterministic source, subset, and selected-name FASTQ records now require the exact `name`, `bytes`, `md5`, and `sha256` field inventory before content comparison. Missing MD5 or SHA-256 values fail closed rather than weakening verification according to the fields supplied by an untrusted manifest.
- Fresh packet extraction now separately requires schema `1.0`, the expected provenance type, and the exact GM11906 or GM12878 dataset identity for both alignment manifests and the deterministic FASTQ-subset manifest. Nine rehashed mutations spanning all three identity fields and all three nested records are rejected.
- The focused adversarial selection passed `39/39`; the complete provenance, packet, and tracked-report surface passed `237/237` in 271.27 seconds; and the complete mandatory suite passed `797/797` in 420.51 seconds. These are working-tree checks only. A successor exact commit, package/smoke reruns, three fresh PASS audits, and the later GitHub/clean-room gates remain mandatory.

## 2026-07-22 - Complete mitochondrial-depth numerator gate

- Candidate `c260ce2484838bf89f2ae0e39fbdf2a0f1737f18` (tree `b7abda43327df5bcdeb92469120e87464477122b`) passed `797/797` exact-tree tests, separate wheel/sdist installations, three installed-package helper probes, and all four installed-wheel workflow smokes. Release-engineering review returned no blocker, but independent bioinformatics review correctly held the candidate after reproducing a scientific input-invariant defect; a third review produced no verdict and is not counted.
- The reproduced case supplied a one-row mitochondrial depth profile for a ten-base reference. The module averaged that partial row and emitted `status=ok` with a numeric ratio, although the documented numerator is the whole-mitochondrion mean. Candidate `c260ce2...` was therefore withdrawn before push despite its passing predefined suite.
- The numerator now requires exactly one finite, nonnegative depth value for every integer position `1..MT_LENGTH`. Missing input remains `no_mito_depth_evidence`; present but partial, duplicate, out-of-range, fractional-position, nonnumeric, negative, nonfinite, or extra-row input is `not_evaluable/incomplete_mito_depth_profile`, with empty mitochondrial mean and ratio.
- The first focused invocation stopped at `3 failed, 12 passed` because pandas 3.0 rejected malformed fractional, string, and infinite test values while constructing integer-typed fixture columns; those cases had not reached workflow code. The fixtures were made explicitly object-typed, after which nine negative cases and the original `100/10=10.0` known answer passed in the `16/16` copy-number suite. The connected reference/configuration/example-builder/tracked-output surface passed `74/74` without valid tracked-output drift, and the complete mandatory suite passed `806/806` in 393.20 seconds. A new exact commit, package/smoke reruns, and three fresh PASS audits remain required.

## 2026-07-22 - Circularity and nested-MD5 audit remediation

- Exact candidate `2fb08a3b741e5ee603be075bdd1d8054d4211a18` (tree `aa761f24cfe8ff654714a3a0fd0c17b5271f8a15`) passed `806/806` tests, separate exact-archive wheel/sdist installation, and all four installed-wheel smoke modes. Fresh release-engineering and reproducibility reviews returned no blocker, but the independent bioinformatics review held the candidate on two reproduced fail-closed defects. The candidate was not pushed.
- Circularity QC converted numeric positions to integers before validating the coordinate inventory, so `100.9` could be treated as position `100`; it also allowed negative or nonfinite depths to retain `status=ok`. The evaluator now requires exactly one finite integer coordinate in `1..MT_LENGTH`, a finite nonnegative depth at every coordinate, and finite calculated regional means. Malformed present evidence remains visible but produces `not_evaluable/incomplete_depth_profile` with `NA` means.
- Validation-packet public-input records allowed the nested deterministic-subset manifest to omit MD5 because the generic digest validator treated MD5 as optional. The builder now requires the exact `label`, `name`, `bytes`, `md5`, and `sha256` inventory for every public input, verifies content MD5 as well as SHA-256 and byte count, and emits an extracted verifier that independently enforces the same inventory and recomputes the nested manifest MD5.
- Five circularity malformed-profile cases passed `10/10` complete circularity tests. The complete pre-freeze packet suite passed `215/215`; six direct missing, extra, and wrong-MD5 nested-manifest attacks also passed after the explicit digest-recomputation cases were added. The connected scientific/report/example surface passed `133/133`, and release hygiene passed `33/33` plus a 433-file scan. Complete exact-candidate validation and three replacement audits remain pending and must not be inferred from these pre-freeze results.

## 2026-07-22 - Nonfinite scientific-evidence audit remediation

- Froze candidate `a0131cf899ab4cacaa604413c121cbe96ce40764` (tree `a0231905af208d78f34f177fb657009d9436e427`) with `paper/**` unchanged at `bfb5664db9c8b43ed5de33ecbddef88071fc6378`. The exact commit passed all `817` repository tests in 402.35 seconds; the extracted source distribution independently passed the same `817` tests in 429.98 seconds.
- Built and separately installed the exact wheel and source distribution outside the checkout. Both imports resolved from their own `site-packages`, exposed the same 18-step registry, passed CLI listing and strict generic dry-run, and completed long-read, reduced short-read, no-methylation, and standalone BAM/CRAM workflow smokes. Exact example rebuilds matched all 88 tracked long-read files and all 74 tracked short-read files byte-for-byte.
- Three read-only role audits were bound to the exact commit. Release engineering (`release-engineering-a0131cf-qjRcbJ`) and reproducibility (`08e9f281-89bf-4545-9cc4-04477dd6bff7`) returned PASS. Bioinformatics (`bioinformatics-20260723T024850Z-1f72d1e2-6cef-468c-8a37-1a64824e8cc4`) returned HOLD after reproducing three scientific defects. Candidate `a0131cf...` was therefore rejected before push; role-specific PASS results are retained as audit history but cannot approve a changed tree.
- Reproduced and corrected nonfinite bedMethyl behavior. `nan`, `inf`, and `-inf` in serialized numeric fields now fail with source/line/field diagnostics; required coverage and count columns must be finite, and coverage/counts must be nonnegative. Legacy omitted other-modification counts retain only the documented nonnegative-residual reconstruction. Malformed counts can no longer become a count-weighted or coverage-weighted observed zero.
- Reproduced and corrected the NUMT interpretation defect. MAPQ must be a finite integer in `[0,254]`; SAM value `255` is unavailable rather than high quality. Aligned-reference and soft-clip fractions must be finite in `[0,1]`; aligned-reference bases must be a finite nonnegative integer; and SA-tag, primary, and supplementary indicators must be exact binary values. Any invalid required field makes whole-genome interpretation `not_evaluable/numt_read_stats_invalid_values` with `NA` risk and score. A malformed primary indicator retains its more specific existing reason.
- Reproduced and corrected optional circularity evidence handling. Candidate and primary-read coordinates must be finite one-based integers within the mitochondrial reference, the primary indicator must be binary, and soft-clip fractions must be finite in `[0,1]`. Invalid candidate, start, end, combined soft-clip, or primary-indicator evidence now yields a metric-specific `NA/not_evaluable` reason and zero evaluable denominator. A complete valid depth profile remains independently `ok`.
- The first focused command used the base Conda interpreter, which lacks pytest, and performed no tests. The pinned environment then reached the new logic; six failures were confined to pandas 3.0 test-fixture dtype injection and one superseded reason-code assertion. After serializing malformed columns through object-typed fixtures, updating the explicit reason expectation, and adding `NaN` cases for every NUMT evidence domain, all `94/94` focused tests passed. A corrected connected selector passed `59/59`; compileall and `git diff --check` also passed. Two exploratory connected selectors named nonexistent historical test files and exited before collection; they are not counted as evidence.
- The successor remains a working tree. It still requires documentation freeze, a complete exact-commit test/package/smoke/example/hygiene rerun, and three entirely new read-only audits before any GitHub push.

## 2026-07-22 - Candidate-table, provenance-identity, and prerelease-state remediation

- Candidate `454c0b93e04069ffd219da0a97a00fecaf17f839` was rejected before push despite completing its predefined execution gates. Three new exact-tree audits all returned HOLD: scientific audit `mito-overview-v0.3.0-20260723T032135Z-c6509d0a-44d3-4ac8-83d0-ff8d8db54d83`, reproducibility audit `12603a81-2120-4f20-b390-9739ec91f9bc`, and release audit `mitooverview-v0.3.0-20260723T032145Z-121646d6-71dc-4c3c-a273-b74c3cfc0409`. Their findings are retained as rejection evidence and cannot approve a successor tree.
- Scientific remediation now rejects nonfinite configuration and allele-policy thresholds; excludes noncanonical reference positions from candidate-evaluable scope; strictly validates candidate, feature-overlap, all-site, and fingerprint tables; propagates unavailable upstream candidate state through co-segregation; rejects out-of-reference CIGAR deletions and fractional bedMethyl counts; makes reversed circularity read coordinates non-evaluable; and withholds categorical NUMT interpretation for MAPQ `255`, inconsistent aligned-base fractions, or missing/invalid required QC fractions.
- Reproducibility remediation now requires complete exact name/size/SHA-256 identities for every alignment, index, reference, reference index, and deterministic-subset derivative, with MD5 additionally mandatory for public FASTQ inputs. Partial, extra-field, wrong-name, label-only, or empty identities fail before cached evidence can be reused.
- Release remediation now requires the GitHub API `prerelease` field to remain the literal Boolean `false` before upload, after upload, and after publication. A draft that drifts to prerelease state cannot produce a passing publication receipt.
- Focused and connected remediation checks passed `327/327`. A preliminary complete checkout run reached `910 passed` with three generated-fixture differences: two tracked example bundles required only the new heteroplasmy/NUMT summary rows and their HTML derivatives, and one historical NUMT fixture required a consistent aligned fraction plus its mandatory QC metric. After those expected corrections, the complete working-tree suite passed `913/913` in 425.05 seconds. These are pre-freeze working-tree results only.
- The frozen `paper/**` tree remains `bfb5664db9c8b43ed5de33ecbddef88071fc6378`. The resulting exact commit must pass complete source-distribution, isolated package/workflow/example/hygiene gates, and three entirely new read-only audits before GitHub can be updated; the pre-freeze `913/913` run alone is not release approval.

## 2026-07-23 - Second exact-audit scientific and publication-transition remediation

- Exact candidate `87bea3bb9666b1f838bd2ab231c5c3490faab54c` passed `913/913` in its checkout and extracted source distribution. Its exact archive built separate wheel and source distributions; both were installed outside the checkout, exposed 18 CLI steps, and passed strict dry-run. The installed wheel completed long-read, reduced-short-read, no-methylation, and standalone BAM/CRAM workflows, and rebuilt the tracked 88-file long-read and 74-file short-read examples byte-for-byte.
- The candidate remained rejected and was not pushed. Release-engineering audit `MO-AUDIT-20260723T044516Z-e4551c15-4d61-4d14-a811-3b92a77dfd15` returned HOLD after reproducing release-identity and asset drift immediately before the irreversible publication transition. Scientific audit `mitooverview-87bea3b-20260723T044458Z-5daf86fc-6c1d-4fc4-a00f-dd8a972f6695` returned HOLD after reproducing partial or duplicate candidate-table acceptance and inconsistent NUMT read/upstream-QC evidence that still yielded categorical output. Two provenance-review attempts ended in agent-service errors and produced no verdict; they are not counted as validation evidence.
- Candidate evidence now requires the complete generated 14-column schema. Both fraction fields must be independently finite, bounded, mutually concordant, and consistent with `alt_count/callable_depth`; depth, strand, and A/C/G/T identities must be exact; keys must be unique; and the one-selected-alternate counting model permits only one candidate row per position. Partial tables and malformed compatibility aliases are no longer repaired.
- NUMT warning evidence now requires `query_length`, `softclip_bases`, and `is_secondary`; primary/secondary/supplementary flags must be mutually consistent; soft-clip counts must be bounded by query length and agree with the reported fraction. The upstream near-complete-alignment fraction is trusted only when the QC module status and metric status are `ok` and its denominator is exactly `primary_alignment_records`.
- The final GitHub publication PATCH now restates the exact tag, final commit, release name, `draft=false`, and `prerelease=false`. The complete asset inventory returned by that transition is normalized and compared before `github_publication.json` is written. GitHub does not provide a server-side asset-digest conditional, so a later mutation can be detected by the existing postpublication requery/download checks but cannot be prevented atomically.
- Focused integration passed `81/81` candidate/annotation tests, `91/91` NUMT/reference/report tests, and `117/117` release/publication tests. The complete pre-freeze suite passed `951/951` in 476.34 seconds without valid tracked-output changes. The frozen `paper/**` tree remains `bfb5664db9c8b43ed5de33ecbddef88071fc6378`.
- This is not yet release approval. A new exact successor commit must repeat exact-archive, extracted-source, installed-distribution, workflow, example, and hygiene gates and receive three fresh role-separated PASS audits before PR #3 is updated.

## 2026-07-23 - Dominant-alternate and read-geometry audit remediation

- Exact candidate `b2f520dc919b4b32dc209b7058ac753b2149fada` passed its complete checkout/archive/source-distribution and installed-package execution gates, but scientific audit `MO-SCI-AUDIT-20260723T055200Z-b2f520d` returned HOLD. Release-engineering audit `MO-RELENG-B2F520D-20260723T060629Z` and reproducibility audit `MO-REPRO-20260723T060135Z-b4854555-793e-4db7-b67a-de2fac4d8566` passed only their bounded roles. The scientific HOLD rejects the candidate, and no verdict is transferable to a changed tree.
- The reproduced defects were exact contract gaps: the named alternate did not have to be a largest non-reference count; mvTool silently deduplicated malformed candidate evidence and circularity inspected only candidate coordinates; and aligned-reference plus soft-clipped query-consuming bases could exceed query length while preserving categorical NUMT output. The original nominal NUMT fixture itself contained 7,000 aligned plus 2,000 soft-clipped bases for an 8,000-base query.
- The successor working tree requires a largest observed non-reference alternate count, applies the complete 14-column candidate contract in mvTool and circularity before interpretation or network-session creation, enforces `aligned_reference_bases + softclip_bases <= query_length`, and corrects the nominal fixture. Exact negative tests cover non-dominant alternates, missing/duplicated downstream candidate evidence, and impossible read geometry.
- The focused remediation suite passed `113/113`; Python compilation and `git diff --check` passed. The complete pre-freeze suite passed `957/957` in 425.57 seconds without valid tracked-output drift. These are working-tree results only. A successor exact commit must repeat all archive, distribution, installed-package, workflow, example, hygiene, and three fresh independent audit gates before any GitHub push.

## 2026-07-23 - Empty candidate-schema audit remediation

- Exact candidate `7a66bfc92208cb454a7c56cf9dea8408a639cea1` passed `957/957` tests from both its Git archive and extracted source distribution, isolated package installation, four workflow smokes, deterministic example regeneration, and local adversarial probes. Reproducibility and release-engineering audits returned PASS for their bounded roles, but scientific audit `MO-SCI-20260723T064328Z-df987b6f-9996-4e99-adad-9343a5dc1a33` returned HOLD. The candidate was rejected before push and no role verdict transfers to modified code.
- The reproduced edge case distinguished schema validity from row count. mvTool returned an empty-result status before validating an existing zero-row table, while circularity's generic optional loader replaced an empty malformed schema with expected columns. A partial-header zero-row candidate file could therefore bypass the complete 14-column contract despite nonempty malformed rows being rejected.
- The successor validates every existing candidate file before empty handling and preserves the candidate file's original columns. A missing file or valid complete 14-column header with zero rows remains an explicit no-candidate state; zero-byte and partial-header files fail closed. Header-only adversarial tests for both consumers bring the focused suite to `116/116`; the complete pre-freeze suite passed `960/960` in 452.70 seconds without valid tracked-output drift.
- The audit also identified superseded GM11906 totals in the frozen manuscript. The approved GitHub phase forbids `paper/**` changes, so this is recorded as a mandatory later manuscript correction rather than silently editing the frozen tree. Complete successor validation and three fresh PASS audits remain required before push.

## 2026-07-23 - Upstream-provenance and hosting-fallback remediation

- Exact candidate `2b981a018629640b81f6a7eb8ec1accab370cbc8` was rejected before push. Its reproducibility audit passed, but release-engineering and scientific audits independently returned HOLD. The release audit found an unsupported GitHub immutable-release endpoint had no approved fallback and the sdist README linked relatively to excluded `paper/**` files. The scientific audit reproduced categorical Phy-Mer output from failed, duplicate, position-zero, and reference-mismatched allele evidence, plus stale mvTool/circularity candidate interpretation after upstream failure.
- The successor removes heteroplasmy-owned tables, report, and figures before recounting; dependent modules require a controlled successful upstream status. Phy-Mer now validates complete one-based `1..MT_LENGTH` coverage, the full 14-column all-site identities, and REF-to-FASTA agreement without silent deduplication. mvTool and circularity distinguish missing candidate evidence from a valid complete-schema zero-candidate observation and never create a request session from nonpassing upstream evidence.
- GitHub immutable releases remain preferred and strict when available. A 404 during both discovery and enablement activates only the recorded `annotated_tag_and_verified_asset_hashes` fallback; annotated-tag identity, final commit, asset inventory, authenticated redownloads, and SHA-256 checks remain mandatory. README repository-only links are absolute and pinned to `v0.3.0` for portable source distributions.
- Focused scientific tests passed `118/118`; the connected scientific, publisher, report, and sdist suite passed `158/158`. After regenerating only the eight provenance-dependent example artifacts, two independent checkout-mode builds were byte-identical across the 88-file long-read and 74-file short-read bundles. The complete pinned suite passed `974/974` in 440.92 seconds, and all four installed-package smoke modes passed, including minimal standalone BAM and CRAM. These remain pre-freeze working-tree checks: exact archive/distribution validation, three fresh exact-SHA audits, GitHub CI, and public clean-room reproduction are still required.

## Rejected `223148c` and callable-consensus/run-freshness remediation

- Exact candidate `223148c88cac9f483acc9550028ed31c179fb4ed` passed its predefined `974/974` checkout/archive/source-distribution and installed-package gates but was rejected before push. Release audit `DA43BF39-6609-434E-AC89-23D37176298A`, reproducibility audit `MO-REPRO-20260723T091606Z-21a16b95-6d37-4081-86f1-0333ed888412`, and scientific audit `MT-AUDIT-223148C-20260723T085745Z` all returned HOLD; no prior PASS or execution result approves a changed tree.
- Publication fallback detection now requires an explicit HTTP 404 on the initial immutable-feature query. Ambiguous 403/500/host errors fail closed, and a failed enablement request cannot downgrade a previously supported endpoint. Report language distinguishes native immutability from the bounded annotated-tag/hash fallback.
- Phy-Mer eligibility now uses the fraction of positions with `callable_depth >= PHYMER_MIN_DEPTH`, defaulting to a minimum fraction of `0.95`. Below-threshold evidence is `not_evaluable` and never invoked; eligible residual low-depth bases are `N`, finite scores are mandatory, and a fully callable reference-supported consensus does not require an arbitrary alternate variant.
- Normal executions now require fresh, nonoverlapping run and final directories, including direct sync protection. Dry-run validates and plans without writing context or creating a run. Focused release/scientific/configuration/freshness tests passed `122/122`; the complete pinned working-tree suite passed `995/995` in 446.64 seconds. A current-tree wheel installed outside the checkout passed all four workflow modes. Two independent long- and short-read example regenerations were byte-identical; only the expected Phy-Mer summary, masked consensus FASTA, and page 13 changed. Exact-commit and replacement-audit gates remain pending.

## Release-byte identity and native-immutability remediation

- Exact candidate `95691642839befa9d3abc935ef0e2c781a783ffd` was rejected before push. Release-engineering audit `mito-v030-release-audit-20260723T112050Z-360ac52d-b537-4e7c-8022-c21f79614b45` and reproducibility audit `93c5d98d-dd20-4e6a-9f28-2bda150a17e6` returned HOLD; scientific audit `MITO-V030-9569164-1db1ea4a-35f7-4e52-af9b-d2566878e12a` returned a bounded PASS that does not transfer. The candidate's `1009/1009` tests and exact package/workflow checks did not waive either release blocker.
- GitHub immutable-release `GET 404` is now represented as supported but disabled. Publication requires successful `PUT` enablement and authenticated re-query confirmation, with no annotated-tag fallback. Failed enablement or confirmation stops before release mutation.
- The exact packet wheel and source archive are now the only installable and publishable distribution bytes. The assembler copies those bytes into the release source, and fresh public-tag validation rebuilds distributions solely for safe member-level comparison. Canonical inventory, member paths, project root, metadata files, payload hashes, sizes, and executable state are enforced; unsafe paths, links, special files, case collisions, substitutions, or extras fail closed.
- Python compilation, shell syntax, and the connected adversarial packet/publisher/tag gate passed `370/370` in 340.01 seconds. The complete pre-freeze repository suite then passed `1038/1038` in 452.80 seconds with no generated-output or `paper/**` drift. This remains working-tree evidence until a successor commit passes the exact archive/package/workflow validation and three new role-separated audits.

## Exact-package sdist metadata correction

- Exact candidate `5287009917db16b4a9bbd14e2fb5caae951e7ef2` was rejected before push after its required second-build comparison failed. Before that failure, its Git archive and extracted sdist each passed `1038/1038`; both isolated installed package forms listed 18 steps and passed long-read, reduced-short-read, no-methylation, and standalone BAM/CRAM workflows; and two independent example builds matched all 88 long-read and 74 short-read tracked files byte-for-byte.
- The failure was reproduced as an over-strict comparator assumption. Setuptools writes both `mito_overview-0.3.0/PKG-INFO` and `mito_overview-0.3.0/mito_overview.egg-info/PKG-INFO`; the release contract needs the former but must permit the standard latter file.
- The comparator now requires the canonical root metadata path while retaining fail-closed root, path, link, special-member, collision, size, payload, and executable-state checks. Its adversarial tests passed `11/11`. The two real builds then matched all 40 wheel and 422 sdist members; the wheel was byte-identical and the sdist differed only in ignored container metadata. Connected affected tests passed `176/176`, and the complete replacement working tree passed `1039/1039` in 432.48 seconds. A new exact successor and fresh role audits remain mandatory.

## Exact candidate `616f798` audit remediation

- Exact candidate `616f798794931475b5c2abe8352b2520073f934f` was rejected before push. Reproducibility audit `MITO-RO-20260723T160529Z-241270f0` returned a bounded PASS. Release-engineering audit `MITO-REL-20260723-616F798-0C7415A4-D0D6-4ED9-A47D-5639BF2E9570` returned HOLD because the GitHub release runner still enforced a manuscript-tree hash despite manuscript inputs being outside this phase. Scientific audit `MITO-SCI-616f798-20260723T160618Z-39adb37f-344b-41a1-8048-6a0de28f0925` returned HOLD because a synthetic Phy-Mer wiring fixture could become a formal haplogroup assignment and a nonpositive circularity edge window could leave misleading evaluable output. No verdict transfers to modified code.
- The successor removes all `paper/` tree checks from the GitHub release runner without editing `paper/**`. Identity QC now retains synthetic Phy-Mer execution evidence but marks its formal haplogroup assignment `not_evaluable`, records `synthetic_phymer_fixture_not_biological`, and suppresses the formal best match. Circularity now rejects boolean, zero, and negative edge windows as invalid configuration.
- The focused remediation suite passed `74/74`. The first complete pinned run passed `1041` tests and found one expected stale artifact pair: the tracked long-read identity summary and page 09 still displayed fixture haplogroup `H1a1`. Regeneration changed only those two files among the 88-file long-read bundle; the 74-file short-read bundle was unchanged. The clean complete rerun passed `1042/1042` in 472.39 seconds. All exact-successor archive, package, workflow, example, hygiene, and fresh audit gates remain required before GitHub push.
